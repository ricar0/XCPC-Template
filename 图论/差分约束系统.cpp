/*
1.求s[n] - s[1]的最小值时，转换为1到n的最长路模型

2.求s[n] - s[1]的最大值时，转换为1到n的最短路模型

ep：
求最短路时
1. xi + T >= xj --> i 向 j 建一条长度为T的边
2. xi + T > xj  --> xi + T-1 >= xj  i 向 j 建一条长度为T-1的边
3. xi + T == xj  --> x + T >= xj && x + T <= xj (同(1))

求最长路时, 将 >= 换成 <= 即可
*/